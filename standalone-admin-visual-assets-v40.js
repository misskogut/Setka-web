(() => {
  "use strict";
  if (window.__SETKA_ADMIN_VISUAL_ASSETS_V40__) return;
  const API="https://gfchgaphzhxufwdhrcis.supabase.co/functions/v1/setka-visual-assets-v40";
  const KEY="sb_publishable_1jL-x9_kp6rpfGghpSp_OA_OiXDnvsv";
  const ADMIN_KEY="setka-research:admin-key:v1";
  const MAX_FILE=2*1024*1024;
  const names={
    "tentacle-orbit":"Tentacle Orbit","dandelion":"Одуванчик","fish-wave":"Носовая волна",
    "breathing-fractal":"Breathing Fractal","breathing-fractal-growth":"Breathing Fractal · Growth",
    "rgb-glitch-rings":"RGB Glitch Rings","stereo-dna":"Stereo DNA"
  };
  let items=[],busy=false;
  const esc=v=>String(v??"").replace(/[&<>\"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m]));
  const adminKey=()=>localStorage.getItem(ADMIN_KEY)||"";
  async function post(body){const r=await fetch(API,{method:"POST",headers:{"Content-Type":"application/json",apikey:KEY},body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`http_${r.status}`);return d}
  async function load(){try{const d=await post({action:"policies"});items=d.items||[];render()}catch(e){console.warn("SETKA visual asset policies load failed",e)}}
  async function save(row){if(busy)return;busy=true;try{const key=adminKey();if(!key)throw new Error("admin_key_required");const patternId=row.querySelector("[data-va-pattern]")?.value?.trim()||row.dataset.patternId;const tier=row.querySelector("[data-va-tier]")?.value||"derived";const imagePolicy=row.querySelector("[data-va-policy]")?.value||"cache_only";const canonicalImageUrl=row.querySelector("[data-va-url]")?.value?.trim()||null;await post({action:"admin-set-policy",adminKey:key,patternId,tier,imagePolicy,canonicalImageUrl});await load()}catch(e){alert(`Не удалось сохранить политику: ${e.message||e}`)}finally{busy=false}}
  function fileToBase64(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>{const s=String(r.result||""),i=s.indexOf(",");resolve(i>=0?s.slice(i+1):"")};r.onerror=()=>reject(r.error||new Error("file_read_failed"));r.readAsDataURL(file)})}
  async function upload(row){if(busy)return;const file=row.querySelector("[data-va-file]")?.files?.[0];if(!file)return alert("Выбери изображение");if(file.size>MAX_FILE)return alert("Файл больше 2 МБ");if(!["image/webp","image/png","image/jpeg"].includes(file.type))return alert("Поддерживаются WebP, PNG и JPEG");busy=true;try{const key=adminKey();if(!key)throw new Error("admin_key_required");const patternId=row.querySelector("[data-va-pattern]")?.value?.trim()||row.dataset.patternId;if(!patternId)throw new Error("missing_pattern_id");const policy=row.querySelector("[data-va-policy]")?.value||"cache_only";if(policy==="cache_only")throw new Error("Сначала выбери server_allowed или server_preferred и сохрани политику");const base64=await fileToBase64(file);await post({action:"admin-upload-canonical",adminKey:key,patternId,mimeType:file.type,base64});await load()}catch(e){alert(`Не удалось загрузить изображение: ${e.message||e}`)}finally{busy=false}}
  async function clearAsset(row){if(busy)return;busy=true;try{const key=adminKey();if(!key)throw new Error("admin_key_required");const patternId=row.querySelector("[data-va-pattern]")?.value?.trim()||row.dataset.patternId;if(!patternId)throw new Error("missing_pattern_id");await post({action:"admin-clear-canonical",adminKey:key,patternId});await load()}catch(e){alert(`Не удалось убрать изображение: ${e.message||e}`)}finally{busy=false}}
  function rowHtml(x,isNew=false){const canServer=x.imagePolicy&&x.imagePolicy!=="cache_only";return `<div class="card" data-va-row data-pattern-id="${esc(x.patternId||"")}" style="margin:8px 0;padding:12px"><div class="small" style="margin-bottom:8px">${isNew?"НОВАЯ ПОЛИТИКА":esc(names[x.patternId]||x.patternId)}</div>${isNew?`<input data-va-pattern class="input" placeholder="patternId" style="width:100%;margin-bottom:8px">`:``}<div style="display:grid;grid-template-columns:1fr 1fr;gap:8px"><select data-va-tier class="input"><option value="mother" ${x.tier==="mother"?"selected":""}>mother</option><option value="derived" ${x.tier==="derived"?"selected":""}>derived</option><option value="special" ${x.tier==="special"?"selected":""}>special</option></select><select data-va-policy class="input"><option value="cache_only" ${x.imagePolicy==="cache_only"?"selected":""}>cache_only</option><option value="server_allowed" ${x.imagePolicy==="server_allowed"?"selected":""}>server_allowed</option><option value="server_preferred" ${x.imagePolicy==="server_preferred"?"selected":""}>server_preferred</option></select></div><input data-va-url class="input" value="${esc(x.canonicalImageUrl||"")}" placeholder="HTTPS URL канонического изображения · необязательно" style="width:100%;margin-top:8px"><div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:8px"><button data-va-save class="btn">Сохранить политику</button>${!isNew?`<label class="btn" style="cursor:pointer">Загрузить каноническое изображение<input data-va-file type="file" accept="image/webp,image/png,image/jpeg" hidden></label><button data-va-upload class="btn" ${canServer?"":"disabled"}>Загрузить выбранное</button>${x.canonicalImageUrl?`<button data-va-clear class="btn">Убрать серверное изображение</button>`:""}`:""}</div>${x.canonicalImageUrl?`<div class="small muted" style="margin-top:8px;overflow-wrap:anywhere">SERVER ASSET · ${esc(x.canonicalImageUrl)}</div>`:""}</div>`}
  function render(){const host=document.getElementById("tab-pattern-knowledge");if(!host)return;let box=document.getElementById("st40VisualAssetPolicies");if(!box){box=document.createElement("div");box.id="st40VisualAssetPolicies";host.prepend(box)}const html=`<div class="card" style="margin-bottom:14px"><div class="brand">VISUAL ASSET POLICY</div><div class="small muted" style="margin-top:6px">Производные визуалы по умолчанию живут только в локальном кэше и восстанавливаются из рецепта. Материнским и специальным паттернам можно разрешить канонический серверный ассет. Загрузка до 2 МБ, WebP/PNG/JPEG.</div></div>${items.map(x=>rowHtml(x)).join("")}${rowHtml({tier:"special",imagePolicy:"cache_only"},true)}`;if(box.innerHTML===html)return;box.innerHTML=html;box.querySelectorAll("[data-va-row]").forEach(row=>{row.querySelector("[data-va-save]")?.addEventListener("click",()=>save(row));row.querySelector("[data-va-upload]")?.addEventListener("click",()=>upload(row));row.querySelector("[data-va-clear]")?.addEventListener("click",()=>clearAsset(row));row.querySelector("[data-va-file]")?.addEventListener("change",()=>{const b=row.querySelector("[data-va-upload]");if(b)b.textContent=row.querySelector("[data-va-file]")?.files?.[0]?.name||"Загрузить выбранное"})})}
  document.getElementById("refreshBtn")?.addEventListener("click",()=>load());
  document.querySelector('[data-tab="pattern-knowledge"]')?.addEventListener("click",()=>load());
  setTimeout(load,500);
  window.__SETKA_ADMIN_VISUAL_ASSETS_V40__={version:3,load,render,get items(){return items.slice()}};
})();