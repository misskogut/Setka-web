# DEPRECATED NAME · SENIOR BLACKBOX EYE PROTOCOL

STATUS: COMPATIBILITY_POINTER

The President renamed the runtime observer to **Бортовой самописец / Flight Recorder**.

**BLACK BOX** is reserved for the protected SETKA / VERSTAK intellectual system itself.

Canonical Senior protocol:

`SENIOR_FLIGHT_RECORDER_PROTOCOL.md`

Canonical universal recorder protocol:

`ops/SETKA_FLIGHT_RECORDER_V1.md`

Legacy SQL/function names containing `senior_blackbox_*` remain temporarily valid compatibility adapters so active missions do not break. Do not use the old Blackbox terminology in new architecture or user-facing descriptions.
