# DEPRECATED NAME · SETKA RUNTIME BLACKBOX RAW V1

STATUS: COMPATIBILITY_POINTER

The President renamed the runtime observer to **Бортовой самописец / Flight Recorder**.

**BLACK BOX** is reserved for the protected SETKA / VERSTAK intellectual system itself.

Canonical protocol and implementation record:

`ops/SETKA_FLIGHT_RECORDER_V1.md`

Legacy database names `runtime_blackbox_*` remain compatibility aliases where changing them would break installed functions/views. New architecture and documentation must use `flight_recorder_*` terminology.
